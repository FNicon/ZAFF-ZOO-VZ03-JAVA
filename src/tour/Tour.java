/**
 * 
 */
package tour;

/**
 * @author Finiko
 *
 */
public class Tour {
  private int now_x;
  private int now_y;
  
/*  public boolean CheckUp(){
    return((GetVal(now_x,now_y-1)).IsHabitat());
  }
  public boolean CheckDown(){
    return((GetVal(now_x,now_y-1)).IsHabitat());
  }
  public boolean CheckLeft(){
    return((GetVal(now_x-1,now_y)).IsHabitat());
  }
  public boolean CheckRight(){
    return((GetVal(now_x+1,now_y)).IsHabitat());
  }
  public void Checksurrounding(){
    if(CheckUp()){
    	
    }
    if(CheckLeft()){
    	
    }
    if(CheckRight()){
    	
    }
    if(CheckDown()){
    	
    }
  }*/
}
